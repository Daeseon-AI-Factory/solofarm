# Anthropic (required for workflow inference)
ANTHROPIC_API_KEY=sk-ant-api03-...

# Frontend origin for CORS (Next.js dev default is 3002 to avoid binjo's 3000)
ALLOWED_ORIGINS=http://localhost:3002,http://127.0.0.1:3002

# App-level
DEBUG=true
