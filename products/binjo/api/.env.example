# Database — same Supabase PG as Phase 1
DATABASE_URL=postgresql://postgres.xxxx:password@aws-0-region.pooler.supabase.com:6543/postgres?pgbouncer=true
DIRECT_URL=postgresql://postgres.xxxx:password@aws-0-region.supabase.com:5432/postgres
# Set false only for a trusted local/container PostgreSQL that does not offer SSL.
DATABASE_SSL=true

# Auth
# REQUIRED — min 32 chars. The app refuses to start without a strong secret,
# because an empty/short HMAC key lets anyone forge valid admin tokens.
# Generate one with: openssl rand -hex 32
JWT_SECRET=

# Kakao OAuth
KAKAO_CLIENT_ID=
KAKAO_CLIENT_SECRET=
KAKAO_REDIRECT_URI=http://localhost:3000/api/v1/auth/kakao/callback

# AI / STT
OPENAI_API_KEY=         # Whisper (voice → text)
ANTHROPIC_API_KEY=      # Claude (text → structured farm log)

# Korean public APIs (data.go.kr)
KMA_API_KEY=            # 기상청 단기예보 — get from https://www.data.go.kr/data/15084084/openapi.do

# Redis — optional, enables async voice processing via Celery
# Without Redis, voice processing runs synchronously (fine for single-farmer MVP)
REDIS_URL=              # redis://localhost:6379/0

# Supabase Storage (same as Phase 1)
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# App
DEBUG=true
ENABLE_DEV_LOGIN=false
# Required whenever ENABLE_DEV_LOGIN=true. Use at least 16 characters, avoid
# surrounding whitespace, and keep it out of source control. This is a temporary
# gate until Kakao OAuth is enabled.
DEV_LOGIN_ACCESS_CODE=
ENABLE_DIRECT_CHECKOUT=false
ENABLE_RECEIPT_OCR=false
ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001
